apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: marginaljobs.marginal.flatheadmill.com
spec:
  scope: Namespaced
  group: marginal.flatheadmill.com
  names:
    kind: MarginalJob
    plural: marginaljobs
    singular: marginaljob
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              x-kubernetes-preserve-unknown-fields: true
            status:
              type: object
              x-kubernetes-preserve-unknown-fields: true
